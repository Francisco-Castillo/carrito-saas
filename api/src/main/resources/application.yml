server:
  port: 9090
  address: 0.0.0.0

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/carrito_db
    username: admin
    password: admin

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        # Re-enables Hibernate's pre-insert nullability check for columns mapped
        # nullable=false. Hibernate skips it by default when a Bean Validation
        # provider is on the classpath (it defers to @NotNull, which the
        # repository module cannot see, so a null slug would otherwise reach
        # the database silently).
        check_nullability: true

  jackson:
    serialization:
      indent-output: true

# Public origin encoded into every printed QR. The local default only works on
# this machine: QR_PUBLIC_MENU_URL MUST be set to the real public origin for a
# QR to work off-device.
qr:
  public-menu-url: ${QR_PUBLIC_MENU_URL:http://localhost:9090}

# WhatsApp Cloud API inbound webhook credentials. Both values are secrets for
# a real deployment: the app secret is the HMAC-SHA256 key Meta signs every
# webhook body with (verified over the raw bytes before parsing), and the
# verify token is echoed back during the one-time subscription handshake.
# Set WHATSAPP_APP_SECRET / WHATSAPP_VERIFY_TOKEN per environment; the
# defaults only serve local development and match the simulator defaults in
# odd/tasks/tools/whatsapp-simulator.mjs.
whatsapp:
  app-secret: ${WHATSAPP_APP_SECRET:local-dev-secret}
  verify-token: ${WHATSAPP_VERIFY_TOKEN:local-dev-verify-token}

management:
  endpoints:
    web:
      exposure:
        include:
          - health
          - info
          - metrics
          - prometheus
  metrics:
    tags:
      application: carrito-saas
  server:
    port: 9090

logging:
  level:
    root: INFO
    com.carrito: DEBUG

  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] [business:%X{businessId:-}] [user:%X{userId:-}] [corr:%X{correlationId:-}] %-5level %logger{36} - %msg%n"

  file:
    name: logs/app.log
